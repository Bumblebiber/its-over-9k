Add a new concern to the o9k compatibility registry (plugins/o9k-core/compat/registry.json):

- key: "telemetry"
- exclusive: false
- desc: "usage telemetry"

Follow the existing style of the concerns block exactly. The file must remain valid JSON. Change nothing else in the file or the repository.
