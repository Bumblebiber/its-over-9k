name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  node-tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [20, 22]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Run node test suites
        # find+xargs: Node 20 does not expand ** globs; macOS bash has no shopt globstar
        #
        # benchmarks/tasks/ is excluded on purpose: those .test.mjs files are
        # workload fixtures for the benchmark (t4-debug ships a deliberately
        # failing test the agent under test has to fix), not repo tests.
        run: |
          set -e
          files=$(find plugins benchmarks -type f -name '*.test.mjs' \
            -not -path 'benchmarks/tasks/*' | sort)
          test -n "$files"
          # intentional word-split; repo test paths have no spaces
          # shellcheck disable=SC2086
          node --test $files
      - name: Run wait-mailbox shell test
        shell: bash
        run: bash plugins/o9k-roster/scripts/wait-mailbox.test.sh
